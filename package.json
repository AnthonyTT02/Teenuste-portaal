{
  "name": "teenuste-portaal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm --prefix frontend exec concurrently -- \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev",
    "build": "npm run build --prefix frontend",
    "test:backend": "npm test --prefix backend -- --runInBand",
    "test:backend:coverage": "npm test --prefix backend -- --coverage --runInBand",
    "test:regression": "npm test --prefix backend -- regression.workflow.test.js --runInBand",
    "test:frontend:coverage": "npm run coverage --prefix frontend",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "test:all:coverage": "npm run test:backend:coverage && npm run test:frontend:coverage"
  }
}
